[buildout]

parts +=
   debian

[debian]
recipe = collective.recipe.cmd
on_install = true
cmds =
    if [ -f /etc/debian_version ] ; then
        sudo apt-get -y --force-yes install \
             build-essential \ 
             libssl1.0.0 \
             libssl-dev
    fi


