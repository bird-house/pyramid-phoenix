[lxml_dependencies]

parts = 
    lxml_dependencies_debian

[lxml_dependencies_debian]
recipe = collective.recipe.cmd
on_install = true
on_update = true
cmds =
       sudo apt-get install libxml2-dev libxslt1-dev
