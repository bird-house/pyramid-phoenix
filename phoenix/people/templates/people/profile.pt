<metal:block use-macro="main_template">

  <div metal:fill-slot="content">

    <div class="container">
      <div class="row">
        <h2 class="page-header">${profile_name}</h2>
        <div class="col-lg-3 col-sm-3">
          <!--Sidebar content-->
          <ul class="nav nav-pills nav-stacked">
            <li class="${'active' if active=='account' else ''}"><a href="${request.route_path('profile', userid=userid, tab='account')}">My Account</a></li>
            <li class="${'active' if active=='twitcher' else ''}"><a href="${request.route_path('profile', userid=userid, tab='twitcher')}">Twitcher access token</a></li>
            <li class="${'active' if active=='esgf' else ''}"><a href="${request.route_path('profile', userid=userid, tab='esgf')}">ESGF access token</a></li>
            <li class="${'active' if active=='group' else ''}"><a href="${request.route_path('profile', userid=userid, tab='group')}">Group Permission</a></li>
          </ul>
        </div>
        <div class="col-lg-9 col-sm-9">
          <!--Body content-->
          ${panel('headings')}
        </div>
      </div>
    </div>

  </div>
   
</metal:block>
