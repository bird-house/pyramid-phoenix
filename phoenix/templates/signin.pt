<metal:block use-macro="main_template">
 
  <div metal:fill-slot="content">

    <ul class="nav nav-tabs">
      <li class="${'active' if active=='esgf' else ''}"><a href="${request.route_url('signin', tab='esgf')}">ESGF</a></li>
      <li class="${'active' if active=='openid' else ''}"><a href="${request.route_url('signin', tab='openid')}">OpenID</a></li>
    </ul>
    
     <!-- row columns -->
     <div class="row-fluid offset1">
       ${panel('headings')}
     </div>

  
    <!--
    <div>
      <h3><img src="${request.static_url('phoenix:static/images/Germany.png')}">DKRZ ESGF Node</h3>
      <form id="openid" action="/login/openid">
        <input type="text" name="username" placeholder="Your username" value="pingutest"/>
        <input type="hidden" name="id" value="https://esgf-data.dkrz.de/esgf-idp/openid"/>
        <button type="submit" class="btn btn-primary">Sign in</button>
        <span class="help-block">
          Enter your username of your DKRZ ESGF OpenID account
        </span>
      </form>
    </div>
    -->

    <!--
    <div>
      <h3>Demo Login</h3>
      <form action="/login/local">
        <input type="password" name="password"/>
        <button type="submit" class="btn btn-primary">Sign in</button>
        <span class="help-block">
          Enter Password of Demo Account
        </span>
      </form>
    </div>
    -->

  </div>

  <div metal:fill-slot="javascript">
    <script src="${request.static_url('phoenix:static/js/authomatic.js')}"></script>
    <script src="${request.static_url('phoenix:static/js/auth.js')}"></script>
  </div>
</metal:block>
