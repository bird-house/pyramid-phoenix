[settings]
phoenix-csw = true

[pycsw]
recipe = birdhousebuilder.recipe.pycsw
name = pycsw
channels = defaults birdhouse ioos
pkgs = pycsw=1.10.4
prefix = ${settings:prefix}
user = ${settings:user}
hostname = ${settings:csw-host}
port = ${settings:csw-port}
