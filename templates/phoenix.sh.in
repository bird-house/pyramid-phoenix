#!/bin/sh
set -e

USER="${install:user}"
GROUP="${install:group}"

export PATH=/opt/anaconda/bin:"${buildout:bin-directory}":$PATH

exec "${buildout:bin-directory}"/gunicorn --user=$USER --group=$GROUP "${phoenix:cfg}"


